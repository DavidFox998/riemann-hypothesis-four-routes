# Preserved Source Snapshots

These six directories are generated by `scripts/import_sources.py` from exact
Git revisions. They preserve declarations, documentation, certificates, and
repository-local scaffolding while migration proceeds.

Do not edit snapshots. Adapt publication code under `RH/` and retain a
provenance row back to the original declaration.