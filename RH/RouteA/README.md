# Route A: Arakelov Positivity

This package publishes the Arakelov route in proof order. It imports Core and
P5 but no other route. Source declarations live in `upstream/RouteA` and are
promoted into this namespace only with provenance and build coverage.