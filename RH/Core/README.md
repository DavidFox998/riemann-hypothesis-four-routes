# RH Core

Core owns the canonical RH vocabulary and publication metadata. Routes import
Core; Core never imports a route. Mathematical, formalization, and source
status remain separate so library availability cannot change mathematical
meaning.